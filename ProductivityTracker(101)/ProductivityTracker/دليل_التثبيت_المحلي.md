# دليل التثبيت المحلي الشامل
## نظام إدارة المشاريع الهندسية

### 🎯 التثبيت التلقائي (الطريقة الموصى بها)

#### الخطوة 1: تشغيل النص البرمجي للتثبيت
```bash
chmod +x setup-local.sh && ./setup-local.sh
```

#### الخطوة 2: تشغيل التطبيق
```bash
# الطريقة الأولى: استخدام النص المحسن
node start-local.js

# الطريقة الثانية: التشغيل التقليدي
npm run dev
```

### 🔧 الحل للمشاكل الشائعة

#### مشكلة "DATABASE_URL must be set"

**السبب**: ملف `.env` غير محمل بشكل صحيح

**الحل**:
```bash
# تحقق من وجود ملف .env
ls -la .env

# تحقق من محتويات الملف
cat .env

# إذا لم يكن الملف موجود، أنشئه يدوياً:
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:password@localhost:5432/consulting_engineers
PGHOST=localhost
PGPORT=5432
PGDATABASE=consulting_engineers
PGUSER=postgres
PGPASSWORD=password
NODE_ENV=development
PORT=5000
SESSION_SECRET=my-local-session-secret-for-development
DEBUG=false
EOF
```

#### مشكلة اتصال قاعدة البيانات

**تحقق من حالة PostgreSQL**:
```bash
# تحقق من تشغيل الخدمة
sudo systemctl status postgresql

# تشغيل الخدمة إذا لم تكن تعمل
sudo systemctl start postgresql
sudo systemctl enable postgresql

# اختبار الاتصال
PGPASSWORD=password psql -h localhost -U postgres -c "SELECT 1;"
```

**إنشاء قاعدة البيانات يدوياً**:
```bash
# إنشاء قاعدة البيانات
sudo -u postgres createdb consulting_engineers

# تعيين كلمة مرور للمستخدم postgres
sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'password';"

# اختبار الاتصال بقاعدة البيانات
PGPASSWORD=password psql -h localhost -U postgres -d consulting_engineers -c "SELECT current_database();"
```

### 📋 التثبيت اليدوي (خطوة بخطوة)

#### 1. تثبيت المتطلبات الأساسية
```bash
# تحديث النظام
sudo apt update

# تثبيت Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# تثبيت PostgreSQL
sudo apt-get install -y postgresql postgresql-contrib

# تثبيت أدوات إضافية
sudo apt-get install -y git curl wget build-essential
```

#### 2. إعداد PostgreSQL
```bash
# تشغيل الخدمة
sudo systemctl start postgresql
sudo systemctl enable postgresql

# تعيين كلمة مرور
sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'password';"

# إنشاء قاعدة البيانات
sudo -u postgres createdb consulting_engineers

# اختبار الاتصال
PGPASSWORD=password psql -h localhost -U postgres -d consulting_engineers -c "SELECT 1;"
```

#### 3. إعداد المشروع
```bash
# تثبيت المكتبات
npm install

# إنشاء ملف البيئة
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:password@localhost:5432/consulting_engineers
PGHOST=localhost
PGPORT=5432
PGDATABASE=consulting_engineers
PGUSER=postgres
PGPASSWORD=password
NODE_ENV=development
PORT=5000
SESSION_SECRET=my-local-session-secret-for-development
DEBUG=false
EOF

# إعداد قاعدة البيانات
npm run db:push
```

#### 4. إنشاء المستخدم الافتراضي
```bash
PGPASSWORD=password psql -h localhost -U postgres -d consulting_engineers << 'EOF'
INSERT INTO users (username, password, full_name, email, role, is_active)
VALUES (
    'admin', 
    '$2b$10$EOmvd8mTzBUN5OJli4H0GeM5AyoRY8Gn25iN.x5rW7ZnQAx3sDd.2', 
    'مدير النظام', 
    'admin@innovators.com', 
    'admin', 
    true
) ON CONFLICT (username) DO NOTHING;
EOF
```

#### 5. تشغيل التطبيق
```bash
# الطريقة المحسنة (تتحقق من البيئة تلقائياً)
node start-local.js

# أو الطريقة التقليدية
npm run dev
```

### 🧪 اختبار النظام

#### اختبار API
```bash
# اختبار تسجيل الدخول
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'

# اختبار الصفحة الرئيسية
curl http://localhost:5000/
```

#### اختبار قاعدة البيانات
```bash
# التحقق من الجداول
PGPASSWORD=password psql -h localhost -U postgres -d consulting_engineers -c "\dt"

# التحقق من المستخدم الافتراضي
PGPASSWORD=password psql -h localhost -U postgres -d consulting_engineers -c "SELECT username, role FROM users WHERE username = 'admin';"
```

### 🎛️ بيانات تسجيل الدخول

| المجال | القيمة |
|---------|--------|
| اسم المستخدم | `admin` |
| كلمة المرور | `admin123` |
| الدور | مدير النظام |
| البريد الإلكتروني | admin@innovators.com |

### 🌐 روابط التطبيق

- **التطبيق الرئيسي**: http://localhost:5000
- **صفحة تسجيل الدخول**: http://localhost:5000/login
- **لوحة التحكم**: http://localhost:5000/dashboard

### 🔍 استكشاف الأخطاء وإصلاحها

#### خطأ "Permission denied"
```bash
sudo chown -R $USER:$USER ~/.npm
chmod +x setup-local.sh
chmod +x start-local.js
```

#### خطأ "Port 5000 already in use"
```bash
# العثور على العملية التي تستخدم المنفذ
sudo lsof -ti:5000

# إنهاء العملية
sudo lsof -ti:5000 | xargs sudo kill -9

# أو تغيير المنفذ في ملف .env
echo "PORT=3000" >> .env
```

#### خطأ "Cannot connect to PostgreSQL"
```bash
# إعادة تشغيل PostgreSQL
sudo systemctl restart postgresql

# التحقق من الحالة
sudo systemctl status postgresql

# التحقق من المنافذ
sudo netstat -tlnp | grep :5432
```

#### خطأ في npm install
```bash
# مسح ذاكرة التخزين المؤقت
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### 📁 هيكل الملفات

```
المشروع/
├── client/                 # الواجهة الأمامية (React)
├── server/                # الخادم الخلفي (Express)
├── shared/                # الملفات المشتركة
├── setup-local.sh        # نص التثبيت التلقائي
├── start-local.js        # نص التشغيل المحسن
├── .env                  # متغيرات البيئة
├── package.json          # المكتبات والتبعيات
└── README.md             # دليل المشروع
```

### ✅ مؤشرات النجاح

ستعرف أن التثبيت نجح عندما:

- ✅ يبدأ `npm run dev` بدون أخطاء
- ✅ يتم تحميل التطبيق على http://localhost:5000
- ✅ يعمل تسجيل الدخول بـ admin/admin123
- ✅ تظهر لوحة التحكم بشكل صحيح
- ✅ لا توجد أخطاء في وحدة تحكم المتصفح

### 📞 الحصول على المساعدة

إذا واجهت مشاكل:

1. تحقق من سجلات وحدة التحكم للأخطاء المحددة
2. تأكد من تثبيت جميع المتطلبات بشكل صحيح
3. تحقق من تشغيل خدمة PostgreSQL
4. راجع ملف `.env` للتأكد من صحة الإعدادات
5. استخدم `node start-local.js` للحصول على تشخيص شامل

**نظام إدارة المشاريع الهندسية جاهز للتطوير المحلي!**